name: simple test

on: [push]

jobs:
  run-shell-command:
    runs-on: ubuntu-latest

    steps:
      - name: echo test
        run: echo "Testing"
      - name: multiline script
        run: |
          echo "line 1"
          echo "line 2"
